package cr.poderjudicial.sigec.incidente.entity;

/** Estados ITIL del incidente (coinciden con el CHECK de incidente.estado). */
public enum EstadoIncidente {
    ABIERTO,
    PROCESO,
    CERRADO
}
