package cr.poderjudicial.sigec.inventario.entity;

/** Estados del ciclo de vida del equipo (coinciden con el CHECK de equipo.estado). */
public enum EstadoEquipo {
    ACTIVO,
    DANADO,
    RETIRADO,
    REEMPLAZADO
}
